
// HIP/SLEEK project directory. Absolute path is recommended
project_directory = /home/evan/hg/sleekex

// SLEEK test directory, relative from project_directory or absolute path
hip_directory = ../../hipci/fixtures/hip

// HIP/SLEEK project directory, relative from project_directory or absolute path
sleek_directory = ../../hipci/fixtures/sleek

// Host address of the Daemon. If you are running as Daemon, set to localhost, then configure TCP forwarding.
daemon_host = "127.0.0.1"

// Port number of the Daemon process.
daemon_port = 2552
